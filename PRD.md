# FloatTodo 产品需求文档（PRD）

> 项目名称：FloatTodo（悬浮待办）
> 版本：v0.1（草案）
> 整理日期：2026-07-26

---

## 1. 项目背景

用户希望拥有一款 **Windows 桌面端待办事项（Todo）工具**，常驻桌面，
通过一个 **可悬浮的“小球”** 形式收纳入口，做到“需要时呼出、不需要时收起”，
不打扰日常办公与学习。

---

## 2. 目标用户与使用场景

- **目标用户**：学生、办公族、自由职业者，需要轻量、随手记录今日/本周计划。
- **使用场景**：
  - 工作中临时想起一件待办 → 拖出悬浮球 → 快速记下 → 收起，不占屏幕。
  - 每日/每周开始 → 打开主面板 → 选定日期范围 → 列出计划 → 勾选完成。

---

## 3. 核心功能范围（MVP）

### 3.1 悬浮球（核心入口）
- 默认以一个 **半透明圆形小图标** 常驻在桌面角落（如右下角）。
- 位置：可在主面板中拖动调整；位置会保存到本地。
- 行为：
  - **单击悬浮球**：展开/收起主面板（带轻动画）。
  - **主面板收起后**：仅剩悬浮球悬浮显示，不抢占焦点。
  - **长按悬浮球 1.5 秒**：进入拖动模式（光标变化），松开后落位。
- 悬浮球本身始终 **最前端显示**，但默认 **不接收鼠标穿透**（可点击）。

### 3.2 主面板
- 一个 **可拖动、可调整大小** 的浮窗（默认 360×520）。
- 包含三大区域：
  1. **顶部 · 日期范围选择器**
     - 提供“开始日期 / 结束日期”两个日期选择控件。
     - 提供快捷选项：**今天 / 明天 / 本周 / 下周 / 自定义**。
  2. **中部 · 待办事项列表**
     - 每条事项结构：`[复选框] 文本内容`。
     - **勾选复选框 → 文本添加删除线**（视觉表示完成）。
     - 已勾选的事项排在下方（可选项，默认开启）。
     - 鼠标悬停显示「删除」按钮。
  3. **底部 · 新增输入框**
     - 单行输入框 + “+”按钮（或回车键）新增事项。
     - 新增的事项自动归属到当前选中的日期范围。

### 3.3 数据存储
- 使用本地文件（SQLite / JSON）保存：
  - 事项列表（含文本、完成状态、所属日期范围、创建时间）。
  - 悬浮球位置、主面板大小/位置、最后选中的日期范围。
- 数据目录：`%APPDATA%/FloatTodo/`（Windows 惯例）。

---

## 4. 交互流程

```
[启动]
   ↓
[显示悬浮球（默认在右下角）]
   ↓
[单击悬浮球]
   ↓
[展开主面板，定位到上次记录]
   ↓
[选择日期范围 → 编辑/勾选/删除/新增事项]
   ↓
[单击悬浮球 / 点击关闭按钮]
   ↓
[主面板收起，回到悬浮球]
   ↓
[右键悬浮球 → 退出]
```

---

## 5. 非功能性需求

| 项目 | 描述 |
| --- | --- |
| 性能 | 启动 < 1.5 秒，悬浮球常驻 CPU 占用 < 1% |
| 体积 | 安装包 < 10 MB（Tauri 优势） |
| 兼容性 | Windows 10 / Windows 11（x64） |
| 离线 | 完全本地运行，无网络依赖 |
| 隐私 | 数据全部本地保存，不上传任何服务器 |

---

## 6. 显式不在 MVP 范围内（暂不做）

按你确认的结果，**以下功能本版本不做**：
- 任务提醒/通知
- 窗口置顶/鼠标穿透切换
- 主题切换
- 任务优先级、标签、子任务
- 同步/云端备份
- 多端协同

后续如需要，再以独立版本迭代。

---

## 7. 技术方案

- **框架**：Tauri 2.x（Rust 后端 + Web 前端）
- **前端**：Vue 3 + Vite + TypeScript（轻量、组件清晰）
- **UI 库**：纯 CSS（手写，无第三方 UI 框架，避免样式冲突）
- **本地存储**：SQLite（通过 `tauri-plugin-sql`）+ 本地 JSON 配置
- **打包**：Tauri 内置 MSI / NSIS 安装包

---

## 8. 目录结构（计划）

```
FloatTodo/
├── src/                  # 前端 Vue 源码
│   ├── components/
│   │   ├── FloatingBall.vue
│   │   ├── MainPanel.vue
│   │   ├── DateRangePicker.vue
│   │   └── TodoItem.vue
│   ├── App.vue
│   └── main.ts
├── src-tauri/            # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── db.rs         # SQLite 操作
│   │   └── window.rs     # 悬浮窗/主窗口管理
│   └── tauri.conf.json
├── package.json
└── README.md
```

---

## 9. 验收标准（MVP 完成的判定）

- [ ] 双击运行后，桌面右下角出现悬浮球。
- [ ] 单击悬浮球，主面板平滑展开；再点击或关闭按钮可收起。
- [ ] 顶部日期范围可选择，能记忆上次选项。
- [ ] 底部输入框回车或点击 + 可新增事项。
- [ ] 勾选复选框，事项文本出现删除线。
- [ ] 关闭应用后再打开，事项、日期、悬浮球位置保持不变。
- [ ] 重启电脑，悬浮球仍能正常显示。

---

## 10. 待你确认的事项

1. 上述功能范围是否符合你的预期？
2. 主面板默认尺寸 360×520 是否合适？
3. 悬浮球默认位置是否放在 **屏幕右下角**？
4. 已勾选完成的事项是否需要提供「清除已完成」按钮？（默认做）
5. 删除事项的方式：仅「鼠标悬停显示删除按钮」是否够用？

---

> 请你过目这份 PRD，回复 **“确认”** 或提出修改意见，我再开始搭项目脚手架与编码。
